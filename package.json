{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "backup": "mkdir -p temp && cp -r ../RobinLiu/.git ../RobinLiu/.gitignore ../RobinLiu/.gitlab-ci.yml temp/",
    "restore": "cp -r temp/. ../RobinLiu/",
    "clean": "rm -rf temp/",
    "build": "npm run backup && astro build && npm run restore && npm run clean"
  },
  "dependencies": {
    "astro": "^4.15.7"
  }
}